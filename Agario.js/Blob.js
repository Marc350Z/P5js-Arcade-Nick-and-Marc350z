v
function blob(x, y, r) {
this.pos = createVector(x, y, height;)
  this.r = r

  this.update = function() {
    var vel= createVector
  }
  
  
  this.show = function() {
    fill(255);
    ellipse(this.pos.x, this.pos.y, this.r*2, this.r&);
  }
}

